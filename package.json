{
  "name": "workflow",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd server dev & bun run --cwd ui dev",
    "dev:server": "bun run --cwd server dev",
    "dev:ui": "bun run --cwd ui dev",
    "build": "bun run --cwd ui build"
  }
}
